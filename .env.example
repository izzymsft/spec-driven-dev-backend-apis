# Environment Configuration for Contoso Customer API

# Azure Cosmos DB Configuration
# Use EITHER connection string OR endpoint (connection string is preferred)
COSMOS_CONNECTION_STRING=AccountEndpoint=https://your-cosmos-account.documents.azure.com:443/;AccountKey=your-primary-key;

# Alternative: Use endpoint with Azure authentication (requires additional setup)
# COSMOS_ENDPOINT=https://your-cosmos-account.documents.azure.com:443/

# Database name in Cosmos DB
COSMOS_DATABASE_NAME=contoso_customer_db

# Optional: Application settings
# UVICORN_HOST=0.0.0.0
# UVICORN_PORT=8000
